#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""The setup script."""

from setuptools import setup

# This setup.py is used for backward compatibility only.
# The actual build configuration is in pyproject.toml

if __name__ == "__main__":
    setup(
        name="nodetool-comfy",
        use_poetry=True,
    )
